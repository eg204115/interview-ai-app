version: "3.8"

services:
  mongodb:
    image: mongo:latest
    container_name: interview_mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: interview_db
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: